#include "shell.h"

/**
 * betty_checks - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int betty_checks(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("Hello, Betty!\n");
	}

	return (0);
}

